<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>coffee.io | Order History</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <header>
        <div class="container">
            <div id="branding">
                <a href="/index"><img src="img/mug.png" title="Home" alt="mug clicked takes you to home page"></a>

            </div>
        </div>
    </header>

    <div class="wrapper">
        <div class="title">
            <h4>Order History</h4> <br>

        </div>
        <% if (user.type == "owner") {%>
        <form action="/export?shopID=<%= shopID %>" method="post">
            <input type="submit" name="upvote" value="Export All" class="button button2" />
        </form>
        <% } %>
        <% orderdata.forEach(order => { %>
        <div>
           <%= order.orderID %>
        </div>
        <% }) %>
    </div>
</body>

</html>