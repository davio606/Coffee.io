<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>coffee.io | Order</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
<header>
    <div class="container">
       <div id="branding">
        <a href="/"><img src="img/mug.png" title="Home" alt="mug clicked takes you to home page"></a>
       </div>
       <nav>
           <u1><% if (!user) { %>
            <li><a href="/login">Sign Up/Login</a></li>
            <% } else { %>
                <li><a href="/signup"><%= user %></a></li>
                <li><a href="/account">Account</a></li>
                <% } %>
            <li><a href="/about">About</a></li>
           </u1>
       </nav>
    </div>
</header>
  
<div class="wrapper">
    <div class="title">
        <h4><%= name %></h4><br>
    </div>
    <div class="menu">
    <% items.forEach(item => { %>
        <div class="single-menu">
            <div class="menu-content">
                <h4><%= item.name %>  <%=item.baseprice%></h4>
                <p><%= item.description %></p>
                <a href = "/chooses?name='+<%=item.name %>'&itemid=<%=item.itemID %>" class = "button button1">Select</a>
            </div>
        </div>
    <% }) %>
    </div>
</div>

</body>

</html>